const MOCK = `/mock`;

// ─── Dashboard ────────────────────────────────────────────────────────────────
export const DASHBOARD_SUMMARY_API           = `${MOCK}/summary.json`;
export const DASHBOARD_EVENTS_TIMESERIES_API = `${MOCK}/events_timeseries.json`;
export const DASHBOARD_EVENTS_API            = `${MOCK}/events_list.json`;
export const DASHBOARD_LOG_LEVELS_API        = `${MOCK}/log_level_distribution.json`;
export const DASHBOARD_TOP_IPS_API           = `${MOCK}/top_source_ips.json`;
export const DASHBOARD_TOP_SERVICES_API      = `${MOCK}/top_services.json`;
export const DASHBOARD_ALERTS_API            = `${MOCK}/recent_alerts.json`;

// ─── Log Explorer ─────────────────────────────────────────────────────────────
export const LOGS_API = `${MOCK}/logs.json`;

// ─── Geo Detection ────────────────────────────────────────────────────────────
export const GEO_SUMMARY_API            = `${MOCK}/geo_summary.json`;
export const GEO_ATTACK_ORIGINS_API     = `${MOCK}/geo_attack_origins.json`;
export const GEO_SUSPICIOUS_IPS_API     = `${MOCK}/geo_suspicious_ips.json`;
export const GEO_ATTACKS_BY_COUNTRY_API = `${MOCK}/geo_attacks_by_country.json`;
export const GEO_DETECTION_EVENTS_API   = `${MOCK}/geo_detection_events.json`;

// ─── Log Report ───────────────────────────────────────────────────────────────
export const LOG_REPORTS_API                 = `${MOCK}/log_reports.json`;
export const LOG_REPORTS_ERROR_SUMMARY_API   = `${MOCK}/log_error_summary.json`;
export const LOG_REPORTS_TRAFFIC_SUMMARY_API = `${MOCK}/log_traffic_summary.json`;
export const LOG_REPORTS_DISTRIBUTION_API    = `${MOCK}/log_distribution.json`;

// ─── Charts ───────────────────────────────────────────────────────────────────
export const CHARTS_API = `${MOCK}/charts.json`;
